/*
 * This is an implementation of the blocks in a standard 2048 game. 
 * Each block is represented by a value, which should accomodate
 * an unsigned integer up to 2048.
 */

typedef struct {
    uint16_t value;
} Block;
